# Gateway Service Configuration
#
# NOTE: When using Docker Compose, all services share a single .env file
# located at services/.env (see services/.env.example)
#
# This file is only needed if running the gateway service standalone
# in local development mode.

# Service URLs (for local development)
QUERY_OPTIMIZER_URL=http://localhost:5000
EMBED_SERVICE_URL=http://localhost:4000
SEARCH_SERVICE_URL=http://localhost:3000
ASK_SERVICE_URL=http://localhost:2000

# Logging
LOG_LEVEL=INFO

# Request timeout in seconds
REQUEST_TIMEOUT=120

# CORS Configuration (comma-separated list of origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
